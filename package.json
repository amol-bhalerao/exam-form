{
  "name": "hsc-exam-form-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "npm --prefix backend start",
    "postinstall": "npm --prefix backend install"
  },
  "main": "server.js",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
