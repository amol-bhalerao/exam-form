{
  "name": "hsc-exam-form-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "npm --prefix backend run dev & npm --prefix frontend run start",
    "dev:win": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run start\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "lint": "npm --prefix backend run lint"
  },
  "main": "backend/dist/server.js",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
