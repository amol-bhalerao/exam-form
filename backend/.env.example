# === DATABASE CONFIGURATION ===
# For LOCAL development:
DATABASE_URL="mysql://root:password@localhost:3306/hsc_exam"

# For HOSTINGER production (example):
# DATABASE_URL="mysql://u441114691_exam:Exam%401234567890@127.0.0.1:3306/u441114691_exam"
# Note: If password has special chars like @ use %40, ! use %21, etc.

# === JWT SECRETS ===
# Generate secure secrets using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_ACCESS_SECRET="your-access-secret-here"
JWT_REFRESH_SECRET="your-refresh-secret-here"
ACCESS_TOKEN_TTL="15m"
REFRESH_TOKEN_TTL_DAYS="7"

# === CORS CONFIGURATION ===
# For LOCAL development:
CORS_ORIGIN="http://localhost:4200"

# For PRODUCTION (uncomment and update):
# CORS_ORIGIN="https://exam.hisofttechnology.com"
